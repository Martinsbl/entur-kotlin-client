query StopPlace($id: String!, $numberOfDepartures: Int, $lines: [ID]) {
    stopPlace(
        id: $id
    ) {
        id
        name
        estimatedCalls(
            includeCancelledTrips: false,
            numberOfDepartures: $numberOfDepartures,
            whiteListed: {lines: $lines}
        ) {
            ...estimatedCallFragment
        }
    }
}
