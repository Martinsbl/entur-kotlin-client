query StopPlaceDetails($stopPlaceId: String!) {
    stopPlace(id: $stopPlaceId) {
        name
        id
        quays {
            id
            name
            lines {
                ...lineFragment
            }
            publicCode
            description
            journeyPatterns {
                directionType
            }
        }
    }
}
