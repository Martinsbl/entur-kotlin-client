query StopPlaceDetails($stopPlaceId: String!) {
    stopPlace(id: $stopPlaceId) {
        name
        id
        quays {
            id
            name
            lines {
                ...linesFragment
            }
            publicCode
            description
            journeyPatterns {
                directionType
            }
        }
    }
}
